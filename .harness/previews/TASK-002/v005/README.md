# TASK-002 v005 · 删除空框

用户反馈“框也删掉”对应本次局部变更：三张设备界面内顶部空状态框、左右信息卡（窄屏下移双列）全部删除。只保留奶油背景、中央盘子食材、主按钮形状和唯一文字“开始下火锅”。安全区无字虚线仅作为评审示意保留。

交付：2186×1645 深色评审板；390×844、320×693、700×1000 三张独立原尺寸入口 Design Composite。画板外边框、项目/版本标题、尺寸和实现说明保留，局部放大改为保留的主按钮。旧版全部保留，未改 Task 或 Unity。

## 来源与限制

延续 v003/v004 的暖色参考与独立原始 PNG：获准的原型 browser_mobile.png（780×1688，390×844 CSS 浏览器模拟），以及原型 food/plate 透明 PNG。评审参考仅定义画板格式，不复用其他项目美术、品牌或功能。当前各视口是布局推演，非设备实测；虚线位置和留白为设计示意，实际运行需使用平台 safeArea。

## 可编辑复现

运行 `python .harness/previews/TASK-002/v005/compose.py`，需要 Pillow 和本地 Windows 微软雅黑。脚本只复用 v003 组件布局，在设备图层中删除所有非主按钮圆角块，并过滤文字为单一按钮标签；画板层不受过滤。执行时 __file__ 固定当前版本，所以全部输出写 v005，旧版源码只读。

`layout-spec.json` 是各视口设计坐标/元素清单；`layer-audit.json` 程序记录每个视口删除的三个空框和保留的唯一文字层。不把此记录当 OCR、视觉批准或游戏 QA。`content-manifest.json` 程序计算输入输出摘要、字节、格式及尺寸。

所有正式 UI 文字仍应使用独立原生控件；预览不能整体裁切为产品 UI。没有新正式资源；字体不随包分发。已目视检查最终评审板，外部生成 0 次，game_qa=NOT_RUN；尚待用户视觉评审。
