# TASK-002 v003 · 入口布局评审板

核心交付：`entry-layout-review-board.png`，2186×1645 深色评审画板，待评审。包含常规 390×844、窄屏 320×693、宽屏 700×1000 三种 Design Composite，并独立输出各原尺寸入口 PNG。均不是实机截图，不代表设备适配或游戏 QA 已通过。

## 参考与保留范围

- 本次格式参考：`.harness/references/TASK-002/review-board-reference.png`，仅使用深色画板、并排视口、版本状态标题、尺寸标题、底部说明与局部放大的交付组织。没有将该参考的像素、美术、品牌、按钮、Logo 或具体适配结论用于本项目。
- 入口沿用 v002 已建立的顶部状态、中央挑战主视觉、两侧非交互信息区、底部唯一开始按钮。奶油底、浅绿信息区、珊瑚主按钮与原始独立食材/盘子素材保持不变。两侧不是新功能入口。
- 暖色视觉来源仍是用户批准的 v0.1 browser_mobile.png 和同恢复包独立 food/plate PNG。原始基准 780×1688，对应浏览器模拟视口 390×844 CSS；实际设备安全区未知。
- 本次允许变化仅为入口视口适配设计和评审板组织，不改玩法/流程语义。v001、v002 未覆盖。正式资产未生产。

## 三种布局

| 设计视口 | 信息区 | 主视觉 | 状态 / 操作 |
|---|---|---|---|
| 390×844 | 盘子左右 | 中央盘子与三种食材 | 顶部状态；底部单个开始按钮 |
| 320×693 | 下移至主视觉下方，保持左右双列 | 缩小并上移 | 状态与底部操作保持可读，缩短中部空白 |
| 700×1000 | 两侧保留，增大间距 | 盘子放大 | 主按钮设置最大宽度，避免无限拉伸 |

虚线/顶部底部留白是人工设置的设计示意。精确示意坐标保存在 layout-spec.json，仅用于复现，不能宣称它们是设备测得的安全边距。实现需要使用平台提供的实际 safeArea，并处理动态日期/字体/文本长度。日期为 2026.09.20 排版示例；所有文字是可控文字层，正式构建应原生 Text 渲染。评审注释不应成为玩家 UI。“侧边内容仅作说明，无交互入口”及安全区示意字样均是评审注释。

## 复现

执行 `python .harness/previews/TASK-002/v003/compose.py`，依赖 Pillow 与本地 Windows 微软雅黑。脚本仅读 v001 的绘制函数定义，所有输出写入 v003；不执行旧版生成逻辑。按组件和视口直接重排，不以整屏缩放冒充重新适配。画板中的局部放大来自本次窄屏生成图，仅辅助说明，三张独立主图才是各视口原尺寸交付。

字体不随游戏分发；食材和盘子复用独立 PNG，无外部图片生成。代码维护布局/文字/色彩/控件边界，可编辑复现；content-manifest.json 由程序计算输入和输出摘要、字节与图片头尺寸。

检查限于本地图片解码、尺寸与目视可读性。外部生成 0 次；game_qa=NOT_RUN，未运行 Unity 或微信，不据 manifest 宣称视觉获批。
