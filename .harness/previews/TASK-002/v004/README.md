# TASK-002 v004 · 仅保留开始按钮文字

用户本轮局部反馈：保留按钮并将文案改为“开始下火锅”；删除设备界面内所有其他文字。交付为待评审 Design Composite，不是真机截图。

## 产物与范围

- entry-layout-review-board.png：2186×1645 深色评审板，三种视口并列。板外标题、版本、尺寸、说明和局部放大标题保留。
- entry-390x844.png、entry-320x693.png、entry-700x1000.png：各自原尺寸入口图。每张仅有一个文字层“开始下火锅”。标题、日期、规则、提示、状态、安全区文字及 Design Composite 字样均从设备界面内删除。
- 保留 v003 的暖色背景、顶部空状态块、盘子/食材主视觉、两侧或下移双列的空装饰卡、主按钮形状和无字安全区虚线。空卡仅装饰，没有按钮或新产品语义。
- 板外仍明确声明全部视口为 Design Composite。线条和留白是设计坐标示意，不是实测安全边距；实际 safeArea 仍需平台提供。

v001/v002/v003 完全保留。本轮没有新增正式美术、游戏代码或 Task 状态写入。参考来源与权限延续 v003：批准的原型画面/独立 food 与 plate PNG 提供暖色视觉，用户评审板参考只提供格式，未复制其他项目内容。

## 复现与图层

执行 `python .harness/previews/TASK-002/v004/compose.py`。需要 Pillow 与 Windows 本地微软雅黑。脚本复用 v003 布局代码，在生成设备层时统一过滤文字调用，仅允许“开始下火锅”；画板层文字不受过滤。旧版代码在当前 __file__ 上执行，所有输出根固定为 v004，不写旧版目录。

`layout-spec.json` 保存视口及设计留白值，`text-layer-audit.json` 自动记录三张设备图唯一文字层调用。它是脚本输出记录，不等同 OCR 或真机验证。主按钮仍采用原生 UI 文字层意图，不烘焙为生产图。字体不随游戏分发。

manifest 的 SHA-256、字节和图片尺寸由程序自动计算。外部生成 0 次。已本地查看合成效果与尺寸，game_qa=NOT_RUN，视觉批准待用户评审。
