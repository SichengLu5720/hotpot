# Task：Daily 确定性玩法内核

> 根据 [tasks/_TEMPLATE.md](C:/Users/charlielu/Documents/ChatGPT/下锅喽/tasks/_TEMPLATE.md) 填写的完整内容草案。拟用编号 TASK-001（原并行方案 DC-A），尚未通过 `workspace task` 创建正式记录，不可派发。
> 当前仓库尚无基线提交，K0/独立 worktree 尚未建立。因此不复制或伪造 `harness-state`；身份、状态、修订、批准和 runs 必须在正式创建后由 `tools/harness.py` 生成。本文件不是第二份机器真源。
> 当前请求已完成的范围是任务文档编制。Task 阶段只定义 QA Intent；正式版本由 Designer 固定 QA Plan，Builder 编写/复用脚本，普通 Runner 执行。

## 0. 任务标识

| 字段 | 内容 |
|---|---|
| Task ID | 拟用 TASK-001；正式 ID 待统一入口登记（本文未生成机器块） |
| task_revision | 未登记；本文为内容草案，不手填机器修订 |
| Status | 未登记；文档草案，不代表 READY / BUILDING / BLOCKED |
| 负责 PM／会话 | 当前主会话；唯一业务协调和用户决定入口 |
| 类型／风险 | Feature / Tooling；High：库存、随机序列、原子事务与胜负耦合 |
| 实现内容 | Code Only |
| 视觉影响 | None |
| 目标版本 | Daily Challenge v1.0 需求范围；Release ID 尚未创建 |
| 创建日期 | 2026-09-20 |

正式状态仅由机器块和统一入口维护：DRAFT、READY、BUILDING、BLOCKED、READY_FOR_RELEASE、CANCELLED。READY_FOR_RELEASE 不等于 QA 通过或发布授权。

## 1. 需求、范围与约束

### 1.1 用户请求与目标

- 原始请求／忠实摘要：用户要求 Unity → 微信小游戏，按三条并行需求拆分，并“按照 md 文件的模板做”三个 Task。
- 当前问题：v0.1 已有路由、暂存和回放原型，但使用 XorShift32、旧 Director、默认满五格失败，缺少每日映射、完整状态机和规范化事件。
- 目标与用户可观察结果：提供与 Unity 场景、微信 SDK 解耦的 Daily 单局逻辑；在固定日期、配置及输入边界下得到可解释、可回放的相同结果。
- 本任务的需求来源和证据身份：

- **S1：Daily SPEC v1.0**：[原文件](C:/Users/charlielu/Documents/Codex/2026-09-20/referenced-chatgpt-conversation-this-is-an/outputs/开锅啦_Daily_Challenge_Core_Gameplay_SPEC_v1.0.md)，SHA256 `F97D61D9B61CE767F73D36252404BBD3156BEED3A788200051D454AA15D97C78`。下文“§”及 CFG/DET/BUF/DIR/SUP 均指该文件。
- **S2：用户决定**：本会话 2026-09-20 指定 Unity，随后明确“微信小游戏”；要求三个需求并行并按 Markdown 模板提供 Task 内容。该请求授权本轮文档编制，不是代码/美术开工或发布授权。
- **S3：拆分依据**：[三任务并行方案](C:/Users/charlielu/Documents/ChatGPT/下锅喽/docs/DAILY_UNITY_WECHAT_THREE_PARALLEL_TASKS.md)。方案中的接口、路径及未决建议尚须固定为正式合同，不能反推为用户已确认产品规则。
- **S4：原型参考**：[原型分析](C:/Users/charlielu/Documents/ChatGPT/下锅喽/docs/PROTOTYPE_v0.1_REVIEW_AND_TASK_DELTA.md)；只读源码基准为 `C:/Users/charlielu/Documents/ChatGPT/下锅喽/.harness/references/prototype-v0.1/HotpotSort_Prototype_v0.1/`。原 ZIP SHA256：`34AFB1587C45506C59F8B9E13785AE0E4487AE13DCF7EC7F632D08B34987F2EC`。
- **S5：恢复证据**：用户恢复包 SHA256 `96b0ff5f2f86b12134f85fb52b1ddebb637b18f9db7a46df453f235214302cff`；局部证据/Approximation/Stub 的边界见 S3 §2。不是可直接导入的完整 Unity 游戏，不覆盖 S1 的产品规则。
- **S6：流程政策**：[AGENTS.md](C:/Users/charlielu/Documents/ChatGPT/下锅喽/AGENTS.md) 与 [模板](C:/Users/charlielu/Documents/ChatGPT/下锅喽/tasks/_TEMPLATE.md)。

### 1.2 功能范围

必须实现：

- 正式导入固定 C 的 50 盘、183 件、16 种及 Difficulty 3 的 20 行权重；校验唯一键、数量、源摘要和样例。
- 接收 TASK-003 的 ChallengeContext，生成 Daily Seed、版本化 PCG32 及 Mapping/Director/Presentation 独立随机流。
- 实现唯一物品位置、订单/暂存/盘子状态；开局前十盘统计；Progress、TempCount 与完整 Director。
- 实现点击预留、路由、逻辑清盘、按槽结算、补单、逐格吸收、连锁保护、严格 Won/Failed/Aborted。
- 输出只读快照、稳定事件、结果统计、回放包及算法版本选择；提供独立于画面的状态构造与命令入口。

- 明确不做：不做 Unity 场景/物理、页面布局、美术、微信 SDK、系统日期获取、上传发布；不接入恢复包隐藏机制、关卡重排或占位方法。
- 必须保留：固定 C 次序及同类关系；三件订单、两启用/四容量、五格不压缩；只有溢出点击失败；表现不改库存或 Director RNG。
- 硬约束：Unity／微信小游戏来自 S2；玩法固定值来自 S1；隔离、视觉批准和集中 QA 来自 S6。未确认具体引擎版本、设备清单、资源预算；不杜撰帧率、包体或网络阈值。
- 已确认前提：Unity、微信小游戏；本轮只编制任务内容。接口/目录是 S3 的方案，需 K0 固定，不把规划建议当成已有实现。
- 来源优先级：S1 的产品定案决定 Daily 行为；恢复证据仅在标注范围提供解释；旧原型的不同规则不构成新验收标准。

### 1.3 业务规则与状态变化

| 规则／场景 | 触发条件与输入 | 预期行为／状态变化 | 异常、空值或限制 | 确认依据 |
|---|---|---|---|---|
| 初始化 | 有效 ChallengeContext 和正式配置 | 校验后构造全新状态、映射、C/H 开局订单；初始订单不消费 Director RNG | 硬校验失败进 Aborted，不随机补数据 | SPEC §5–7、§11、§16 |
| Progress/TempCount | 补单前稳定快照 | 分子为 Completed+当前订单已收；暂存不计；min(bufferCount,4) | 按第一个大于等于 progress 的阈值选档 | §12 |
| Director | 完成槽需要补单 | 严格候选、成本、唯一分类、过滤空类、抽签、六级 fallback | 完整成本采用 Daily 近似；库存公式待 Q-A02 澄清 | §13 |
| 点击 | Running、未锁定、命中有效 ActiveAvailable | 同帧 Reserved；同类单先收件较多、再 slotId；否则最小空暂存格 | 拒绝输入不改库存；失败点击接受/统计语义待 Q-A05 | §9 |
| 满暂存 | buffer 从 4 到 5 / 满时继续点击 | 第五格正常占用；匹配订单仍可接收；无匹配且无空位才 Failed | 溢出不扣原盘，不增加已处理数 | §9.2、§10.3 |
| 连锁 | 订单完成、新订单吸收 | 稳定槽序结算；每次新单按 buffer 0→4 吸收并保留空洞 | 65 上限及计数单位须固定；异常进 Aborted | §9.3、§10.2、§13.8 |
| 供给与终局 | 收到空间许可 / 事务收尾 | 只消费 Pending 队首；183 全完成且各容器清空才 Won | 供给记录边界待 Q-A03；一致性错误不能报胜利 | §8、§14、§17 |
| 重试/兼容 | C 请求重建同日会话 | 新对象恢复原骨架、初始 RNG、统计和事件号；保留旧算法选择 | 不是复用上局可变对象；跨日由 C 已确认合同约束 | §6.4、§25 |


未決产品行为集中列在第 6 节；推荐方案不默认为用户决定。

## 2. 验收意图／QA Intent

下表是合同候选的人工草案；正式确认后通过 `task contract` 写入机器合同。只描述应验证的行为，不是可执行脚本或已固定 Release QA Plan。每项完整标识为 `TASK-001:QI-xxx`。

| Intent ID | 场景／前置条件 | 应验证的结果或受保护行为 | 来源／决定依据 | 版本验证性质 |
|---|---|---|---|---|
| QI-001 | 标准内容与非法内容 | 50/183/16、每类三倍数、连续唯一 plateId；坏配置指出错误且不静默补齐。 | CFG 01–03；§5.2、§16 | 自动化候选 |
| QI-002 | 权重资产 | 20 唯一组合、五项非负整数且至少一正、两个样例与来源摘要匹配；缺行/重复/不匹配阻止构建。 | CFG 04–06；§13.1–13.2 | 自动化候选 |
| QI-003 | 每日映射及随机初始化 | 同日期配置初始化 100 次映射/开局/初始核心事件哈希一致；16 种一一映射；目录不足失败。 | DET 01；§6、§16.5 | 自动化候选 |
| QI-004 | 随机流隔离 | 增加表现流抽取不消费或改变 Director 流；算法/派生和 drawIndex 可观察。 | DET 03；§6.3 | 自动化候选 |
| QI-005 | 重试与不同路径 | 相同接受输入和边界重试复现，retryIndex 按固定哈希合同处理；不同路径允许分叉但守恒。 | DET 04–05；§6.4 | 自动化候选 |
| QI-006 | 开局 | 标准 C 稳定 C/H；前十盘统计；kindId 并列；兜底按 SPEC，不把重复订单当开局默认。 | §11、§20.1 | 自动化候选 |
| QI-007 | 阶段与暂存档位 | 0.40 归 Early、略大归 Mid；其余阈值按 §12；4/5 暂存均选 B4。 | DIR 01–03；§12 | 自动化候选 |
| QI-008 | 成本与类别 | 暂存同类 d<0；无暂存且场上三件 d=0；其余按干扰量；N=0/1 时按优先级唯一分类。 | DIR 04–05；§13.4–13.5 | 自动化候选 |
| QI-009 | 候选及抽签 | 排除不足库存、先严格非重复；空类先过滤，不浪费类别抽签；类别内稳定排序；无严格候选才放宽。 | DIR 06–07；§13.3、§13.6 | 自动化候选 |
| QI-010 | 完整 fallback | 零权重压力排序、重复放宽、超范围最低成本、合法尾局空槽均可解释；仍有库存全空且无合法单则 Aborted。 | DIR 08–09；§13.7 | 自动化候选 |
| QI-011 | 第五格与溢出 | 4→5 继续；5/5 匹配仍入单；无匹配溢出才失败且原盘库存不变。 | BUF 01–03 | 自动化候选 |
| QI-012 | 路由及防重复 | 同种单优先已收较多、并列 slotId；同 item 快速重复只接受首次；拒绝不改库存。 | BUF 05–06；§9 | 自动化候选 |
| QI-013 | 吸收与连锁 | 按暂存索引吸收、不压缩；订单独立补位；稳定事件与连锁边界，无死循环。 | BUF 04；DIR 10；§9.3、§10、§13.8 | 自动化候选 |
| QI-014 | 供给及严格终局 | Pending 或 Active/Buffer/订单仍有未处理食材不胜利；183 完成并清空为 Won，计数冲突为 Aborted。 | SUP 03–06；§14 | 自动化候选 |
| QI-015 | 不变量和错误出口 | 每件唯一位置、总量守恒、订单容量、完成数三倍增量、RNG 所属、不重复盘 ID、终局互斥；错误快照可读。 | §17 | 自动化候选 |
| QI-016 | 事件和回放内容 | eventSeq 从 1 起、事务内稳定排序；接受输入/拒绝诊断分离；Director 原因、权重、候选、抽取及版本/配置身份可还原。 | §15、§18、§25 | 自动化候选 |
| QI-017 | 跨平台逻辑一致 | Windows、Android、微信目标上的黄金回放核心事件和最终哈希一致；平台执行由 TASK-003 配合。 | DET 02；§20.3 | 自动化候选，平台环境待版本计划 |
| QI-018 | 长跑与模糊 | 365 连续日期初始化；合法/随机/压力策略各至少 10,000 局；无库存丢失、负数、死循环、超限或无解释空槽。 | §19.6 | 自动化候选 |
| QI-019 | 统计口径 | 自动吸收不新增玩家点击；自动吸收数、订单数、暂存峰值及失败信息可由事件重建；未决计时/失败点击口径先澄清。 | §10.2、§18.3 | 自动化候选 |


- 需覆盖的边界与失败路径：以上分别保留正常、非法/空值、满格/终局、版本/平台异常的来源，不以“总体正常”合并掉独立断言。
- 跨功能交互：A 对核心预期负责，B 对实际命中/表现负责，C 对会话/平台负责；各相关 Intent 在版本 QA Plan 绑定真实候选，不能用替身断言代替实际集成。
- 用户特别关注项：Unity/微信目标与三线并行（S2）；所有权和接口隔离见第 4 节。
- 人工体验／视觉判断：本任务本身无视觉批准项；体验由 B/C 联合记录，不能以模拟清盘证明体验或可解性。
- 待确认的回归候选：尚无获批的额外回归范围；正式 Designer 可依据最终实现组织来源明确的用例，不自行扩展产品阈值。
- 已知历史问题：原型最小诊断发现 d 分类、重叠类别、缺表兜底与 Daily 不一致；本 Task 尚未实施，不能沿用旧原型通过结果。

不因旧实现恰好如此而改写预期；验收语义变化由 PM 请求用户决定。

## 3. 技术设计与接口（按需）

技术调查：需要一次共享 K0 接口与缺口梳理；不默认对本任务单开完整 Designer QA 代理。 Designer 仅返回只读调查事实，不写脚本；正式版本再统一制定 QA Plan。

### 3.1 当前实现与推荐方案

- 已调查入口：原型 Web/js/core.js（Game、candidateCosts、pickRefill、settle、exportReplay）；Unity/Assets/HotpotSort/Runtime/Core/GameSession.cs、GameData.cs。原型包仅在隔离参考目录，尚未接管为正式工程。
- 固定输入：原型以 S4 ZIP 身份识别；恢复资料以 S5 身份识别。当前没有正式接管提交，不能引用不存在的 commit。
- 当前状态／数据流：上述是源码与先前报告事实，未声明本任务在 Unity/微信内运行通过。
- 推荐实现：保留可用数据结构与固定 buffer/路由思路；拆出纯逻辑模块和不可变快照；按 Daily 重构 Director/RNG/事务；物理与平台通过 Contracts 端口输入事实。
- 可逆内部取舍：模块内实现可调整，跨 Task DTO、行为/资产含义、公开接口不由单方改动；共享契约变化需 PM 协调并固定新基线。
- 数据／接口兼容性：配置和算法带版本；回放必须选择对应旧实现。旧 v0.1 回放不自动声称可作为 Daily 输入，若支持转换需显式版本适配。
- 待用户决定的产品变化：见第 6.1 节；不在本节替用户选择。

### 3.2 功能接口与归属

接口名称均为 K0 草案，尚无实际符号、调用结果或稳定提交。

| 接口／事件／绑定键 | 提供方 → 使用方 | 参数／前置状态 | 返回／可观察变化／失败行为 | 读写性质与授权 | 实现或文档引用 |
|---|---|---|---|---|---|
| ChallengeContext / CreateSession / ResetSession | C → A | 固定日期、内容版本、配置摘要、timeSource、retryIndex | 新会话与初始事件；配置错误明确 Aborted | 修改状态 | 拟定；K0 冻结后实现 |
| TapCommand / TapResult | B → A → B/C | itemId/inputSeq/逻辑边界/命中事实；Running | 接受或原因；稳定事务事件，不由 UI 判断路由 | 修改状态 | §9；拟定 |
| SupplyObservation / SupplyCommit | B → A → B | 空间许可、冷却边界、Pending 可用 | 返回队首 plateId/items 与供给事件 | 修改状态 | §8；回放边界待确认 |
| GameSnapshot / GameEventBatch | A → B/C | 稳定事务完成；版本化 schema | 只读库存、订单、统计、状态及序号 | 查询/订阅 | §15–18；拟定 |
| PresentationRandom | A 表现流端口 → B | 已初始化；调用次数可记录 | 仅表现流结果，绝不消费 Director | 消费表现随机状态 | §6.3；拟定 |
| ReplayPackage / Replay / DiagnosticSink | A → C及版本 Runner | 固定算法/配置/输入身份 | 回放包、状态/核心哈希、错误快照 | 导出查询；回放创建隔离状态 | §18、§25；拟定 |


查询接口与命令分离。只读快照不能暴露可直接变更库存的集合；真实状态变更、日志外部写入和平台构建不作为无副作用查询执行。

### 3.3 实现需要保留的可测试性

- 稳定标识：itemId、plateId、slotId、ingredientId、sessionId、inputSeq、transactionId、eventSeq、内容/算法/配置身份；节点与页面绑定键在 K0 登记。
- 构造入口：隔离实例创建、配置注入、固定输入与边界重放、只读快照。
- 种子／时钟：A 唯一管理核心 RNG；C 提供日期来源；B 只能消费独立表现流。短锁、暂停和供给边界在 K0 固定。
- 可观察输出：逐事务核心状态、Director 明细、错误快照、统计和回放哈希。
- 生产限制：开发替身、任意种子/配置注入、调试入口不能进入正式用户流程；不能隐式替玩家救场。
- 复用线索：原型 `tests/test_core.cjs`、`tests/test_browser.py`、`Editor/ProjectTools.cs` 提供旧用例线索；其中旧规则和自动生成 golden 不能直接充当 Daily 基准。

本节是接口需求，不授权编写或运行完整 QA 套件。

## 4. 功能隔离与并行协作

### 4.1 工作区身份（只读索引）

- Worktree 根目录：尚未创建；正式创建后从机器块读取。
- 分支：拟 `codex/daily-core`，尚未创建。
- 基线提交：K0 待建立；当前仓库无 HEAD，未伪造 SHA。
- 本 Task 内容文件：`C:/Users/charlielu/Documents/ChatGPT/下锅喽/docs/task-drafts/TASK-001-daily-core.md`；它不是 Harness 可派发正式记录。
- 本地 Git 操作：本轮仅写文档；尚未执行提交、合并、推送或创建开发 worktree。

### 4.2 文件和功能归属

以下为拟定写入边界，需随正式合同固定；文档列路径不等于当前已授权实现。

| 负责方 | 交付内容 | 允许写入的路径 | 禁止写入／交接边界 |
|---|---|---|---|
| PM | 任务、用户决定、交接索引 | 本草案；正式任务由统一入口写机器记录 | 不手工写 approvals/runs/状态 |
| Designer | 共享接口只读调查；版本 QA Plan | 通过交接返回 PM，由工具归档 | 业务代码、美术文件、可执行脚本和验收偷改 |
| Design-Art | 不适用：无美术生产 | 无 | 不得编辑 Task、逻辑、最终引擎绑定或场景 |
| Code Builder | 代码与最终绑定；版本阶段按固定 QA Plan 写脚本 | `Unity/Assets/HotpotSort/Runtime/Core/`<br>`Unity/Assets/HotpotSort/Runtime/Determinism/`<br>`Unity/Assets/HotpotSort/Runtime/Replay/`<br>`Unity/Assets/HotpotSort/Content/Daily/`<br>`Unity/Assets/HotpotSort/Editor/ContentImport/` | 不得写其他 Task 拥有路径；Contracts 只读；未获范围的 QA 脚本不先写 |

本任务没有 Art 同路径生产。
共享 Git 索引、Unity 场景、公共目录元数据和工程设置各自保持单写者；不得回退其他工作区的更改。

### 4.3 依赖与共享触点

| 依赖 Task／模块 | 固定修订／提交 | 所需接口或产物 | 集成顺序／是否共同交付 | 协调者 |
|---|---|---|---|---|
| K0 公共基线 | 待建立，尚无真实 commit | 选定工程兼容约束、Contracts/ID/schema、隔离约定、旧 HotpotApp 职责拆分 | 唯一开工代码依赖；不是第四个业务需求 | PM + 单一基线 Builder |
| TASK-002 Unity 可玩场景与玩家界面 | 开发期不依赖其未完成提交；最终固定修订/commit | 真实视图/物理/页面 | 共同版本；早期使用自身范围内的替身，最终接真实固定产物 | PM |
| TASK-003 微信小游戏平台与每日会话 | 开发期不依赖其未完成提交；最终固定修订/commit | 真实会话/平台端口 | 共同版本；早期使用自身范围内的替身，最终接真实固定产物 | PM |


| 共享文件／页面区域／主题 | 本 Task 的安排 | 其他责任方 | 共同基准与协调方案 |
|---|---|---|---|
| Contracts/公共 ID 与 schema | 只读；变更提请 PM | 三 Task 使用，K0 单写者维护 | 尚未冻结；变更固定新提交并处理相关合同失效 |
| 核心状态与内容配置 | 唯一写入者；逻辑目录与视图目录分离 | A | 不得复制库存规则到 B/C |
| Gameplay 场景/全页面主题 | 通过 IGameView 使用，不编辑 B 场景 | B | B 持有全部玩家视觉，C 提供视口事实 |
| Boot/组合/Packages/ProjectSettings | 仅声明依赖，不另起入口或改工程设置 | C | 不用三方共同编辑 HotpotApp 的方式集成 |

接口已固定后 A/B/C 可以并行开发；最终绑定仍串行。依赖只使用固定提交，不读取另一个活动 worktree 的未提交文件。

### 4.4 运行数据隔离

| 对象 | 独立位置／命名空间 | 真正采用它的入口 | 当前情况 |
|---|---|---|---|
| 存档／用户数据 | 无新产品存档功能；测试/会话数据按 TASK-001 和 run 分离 | C 的 RuntimePaths → 本模块实际采用；不沿用共享 persistentDataPath | 未配置，不能称已隔离 |
| 缓存／临时文件／日志 | worktree 专属运行根，具体绝对路径待创建后绑定 | 显式启动配置/日志 sink；需验证模块确实消费 | 未配置 |
| 构建／导出 | 每 worktree 独立输出；正式候选使用 release 根 | C 维护构建入口，导出路径随候选登记 | 未配置；本轮未构建 |
| 账号／端口／外部服务 | 仅必要平台配置；本 Task 不新增共享数据库 | 若实际使用由 C 登记命名空间，不将密钥写入 Task | 服务端点/开发工具项目未固定 |

仅创建目录或设置环境变量不构成适配。隔离未实接前不并行启动会写同一数据根的实例。

## 5. 实机预览、资产概念与生产合同（有视觉改动时）

适用范围：不适用。本任务不生产或修改玩家画面；所有场景与资源由 TASK-002 实现。

### 5.1 实机底图与编辑边界

不适用：无画面修改，无需底图；不据此批准 TASK-002 的参考图。

### 5.2 高精度预览交付

不适用：不生成预览，不能用“无需预览”代替构建授权。

### 5.3 资产概念与生产规格

不适用：无正式艺术资源生产；逻辑内容 JSON 属配置资产，按第 1–3 节校验。

### 5.4 生成预算与交接

不适用：没有 ImageGen 或其他美术生成调用；不产生艺术 Manifest。

## 6. 澄清、批准与变更

### 6.1 未决问题与用户决定

| 问题／冲突 | 受影响范围与可选方案 | PM 向用户反馈的引用 | 用户决定与依据 | 合同／产物影响 |
|---|---|---|---|---|
| 平台/拆分目标 | Unity + 微信小游戏，三个功能包按共同基线并行 | S2 用户消息及平台答复 | 已确认平台与文档拆分目标 | 本草案范围；不等于实现授权 |
| Q-A01：权重整数导入 | 源文件为小数，SPEC 要求整数和原样导入；建议保留源 SHA，100 倍精确转换并记录工具版本。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | 权重构建与两个样例 |
| Q-A02：库存公式与成本预留 | 明确 §13.3 已收件是否被再次计入；建议采用原型的外部库存池减其他单未满足需求；成本如何尊重预留一并固定。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | 候选合法性、尾局 |
| Q-A03：供给与回放边界 | 原型记 spawn/tap；Daily 最小包尚未明确供给许可/时序。明确可重放的供给事件与 v 的逻辑定义。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | A/B/C 共同接口 |
| Q-A04：随机、哈希及扫描协议 | 固定 Seed 串编码、PCG 初始化/抽样、唯一候选 RNG 消费、核心哈希排除字段、最后扫描盘范围。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | 确定性与 Director |
| Q-A05：失败输入与时钟 | 明确溢出是否 accepted、计入哪些点击、Reserved 如何归位；输入短锁时钟及暂停关系。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | 事务、统计、B/C |
| Q-A06：连锁计数 | 65 的计数单位与边界固定；不能用旧 total*4+32 轮守卫替代。 | 前序分析与本草案问题项；尚无对应明确答复 | 待确认 | 异常与连锁 |
| Q-K0：共同基线与正式登记 | 尚无工程基线/工作区/真实接口；先冻结共用合同再并行 | S3；本轮模板核对 | 尚未建立 | 三方开工依赖；不能造假 SHA 或直接派发此文档 |

本轮是文档规划，未派发代码/美术运行。正式任务如遇真实歧义，由 PM 对整个相关 Task 执行 block、请求宿主停止并登记证据；决定后 resume 与新 run，不在等待时做会受决定改变的实现分支。

### 6.2 当前批准依据（机器 approvals 的可读索引）

| 批准范围 | 对应 task_revision | 具体产物或合同 | 用户决定引用 | 当前结论 |
|---|---|---|---|---|
| 本轮文档编制 | 未登记 | 当前三个 Task 内容草案 | 用户：按照 md 文件的模板做 | 已授权，本文为交付 |
| 页面／场景预览 | 未登记 | 不适用：本 Task 无视觉改动 | 无 | 不需要 |
| 必需资产概念 | 未登记 | 不适用：无 Art | 无 | 不需要 |
| 代码／美术构建开工 | 未登记 | 未固定的功能合同/K0 | 无本轮开工决定 | 尚未登记授权 |
| 版本 QA 执行/发布 | 未登记 | 尚无 Version、候选或固定计划 | 无 | 本轮不涉及 |

批准必须绑定真实内容与用户决定，由统一入口登记；不把“给我 Task 内容”写成 build approval。

### 6.3 暂停与恢复（发生时填写）

- 当前没有派发或在途 run；不存在需要取消的游戏实现/生成进程。
- 尚未通过 Harness 执行 block/resume；未决项不伪装成机器 BLOCKED。
- 本轮只写文档，未写游戏文件、生产资源或平台设置，无晚到产物绑定。
- 正式派发后按 run 身份处理停止、拒收、回滚三件事；未知停止状态不得复用同工作区继续写。

## 7. 实现交付与 Release Handoff

### 7.1 已交付内容

- 本轮已交付：按模板整理的目标、范围、QA Intent、接口/所有权草案和缺口清单。
- 游戏实现：未开始；第 3 节的现有入口只是原型参考，不是本 Task 新实现。
- 正式资源、Manifest、批准图：本 Task 无美术产物。
- 场景／平台／模块接入：未执行；开发替身也尚未创建。
- 真实实现提交：无；Task/worktree/run 尚未登记。
- 依赖/共享交接：待 K0 及最终固定 A/B/C 提交；无 accept 回执，不手填 completed。

### 7.2 交付阶段 QA 所需的实现事实

| Intent ID | 实际接口／稳定标识／构造入口 | 可观察字段 | 复用线索／限制／缺失 |
|---|---|---|---|
| 本任务 QI-001～QI-019 | 未实现；第 3.2 节为接口需求草案 | 第 3.3 节定义所需观察范围 | Builder 实现后逐项补真实入口/路径，不修改预期 |
| 跨任务 Intent | 最终真实 A/B/C 候选，不能是替身 | 配置/算法/候选/设备/证据身份 | 由固定版本 QA Plan 指定断言与 fixture |

未实现接口不填写虚构路径或调用成功记录；Builder 只补事实，含义变化回 PM。

### 7.3 已知问题与未验证内容

| 问题／未验证项 | 证据或缺失 | 影响与归因 | 处理／是否阻断 |
|---|---|---|---|
| 旧原型与 Daily 的差异 | 原型最小诊断发现 d 分类、重叠类别、缺表兜底与 Daily 不一致；本 Task 尚未实施，不能沿用旧原型通过结果。 | 来自已阅读旧原型，不归因于尚未开始的新实现 | 按本 Task 范围处理；不能沿用历史 PASS |
| 第 6.1 节未决项 | 缺少明确合同/产品决定 | 影响对应实现/验收含义 | 相关任务就绪前解决 |
| Unity/微信真实运行 | 本任务无运行和构建证据 | 目标平台质量未知 | 正式版本集中覆盖，不声称已验证 |
| K0/隔离/固定候选 | 尚未建立 | 不能直接并行运行或派发正式记录 | 前置基线与真实工作区适配 |


| 本轮检查原因与授权 | 实际范围 | 实际结果 | 仍未覆盖 |
|---|---|---|---|
| 用户要求按模板提供内容 | 模板章节、文档链接/内容与三个所有权边界检查 | 文档交付检查；不属于游戏 QA | 功能测试、Unity 编译、微信运行、人工体验 |

不默认运行固定冒烟、全回归、多分辨率截图或原型的自动重写 golden 脚本。

### 7.4 实现就绪交接

以下项目仅在实际交接后勾选，当前全部未完成：

- [ ] 必需实现及绑定已接收，范围偏离已解决。
- [ ] 必需预览/概念与构建授权覆盖实际采用内容；无未解决歧义或旧写入者。
- [ ] QA Intent 有来源且已进入唯一机器合同，真实接口与未验证项已披露。
- [ ] 真实提交、固定依赖、共享触点及必要产物身份可供版本集成。

PM 交接结论：当前仅文档草案完成，不申请 READY_FOR_RELEASE；正式状态以统一入口记录为准。

## 8. 版本 QA 关联（纳入版本后填写）

| 版本产物 | 责任归属 | 固定引用 |
|---|---|---|
| 纳入的 Task 修订/提交 | PM 固定三个真实 Task | 未纳入，未创建 Release ID |
| QA_BACKLOG | 程序从固定 Task 抽取，PM 核对来源 | 未生成 |
| QA_PLAN | 只读 Designer 制定用例、断言、fixture/reset 与接口需求 | 未制定/固定 |
| execution/脚本/fixture | Builder 严格绑定固定计划 | 未编写或执行 |
| 自动结果 | 普通 Runner 输出逐断言结构化报告 | 无结果；零项/跳过/旧候选不能称通过 |
| 人工证据 | PM 记录真实人工结论 | 未进行；不将编辑器或模拟写成实机 |

脚本与候选固定、明确授权后才执行。任何用例/断言含义变化回 PM→用户，不能借脚本修复删掉标准。候选变化后不自动复用旧证据。

集中版本保留 SPEC 的 100 次初始化、365 日期、三策略各至少 10,000 局，以及跨平台回放和真人设备要求。A 负责核心入口；B/C 负责相应真实适配与观察；不是三个任务各自重复跑一遍整套长跑。QA_COMPLETE 不代表发布授权。

## 9. 合同修订说明（机器 history 的可读索引）

| task_revision | 日期 | 有效内容变化与原因 | 用户／PM 决定依据 | 对产物/批准/运行影响 |
|---|---|---|---|---|
| 未登记（内容草案） | 2026-09-20 | 将 DC-A 填成完整模板文档；尚未建立正式机器合同 | 用户要求三个 Task 内容并按 md 模板制作 | 无 run、无批准失效；不手工设置 revision=1 |

正式创建后通过 `task contract` 写入确认需求并取得真实修订；正文仅解释变化，不独立推进机器状态。第 6 节实质未决项须进入正式合同，不能仅留在聊天中。

## 10. 2026-09-20 实施请求与待决方案

- 用户新指令：`实行task1`。PM 将其记录为 TASK-001 的实施授权；前文“仅文档编制”的授权描述属于历史记录，不再作为重新请求开工许可的理由。
- 本次读取确认：正式 Task 尚未创建，仓库无初始提交，K0 与独立工作区尚未建立。未派发运行，无需停止在途写入者；不能对无机器记录的草案伪造 `BLOCKED` 状态。
- 当前交接结论：`NEEDS_CLARIFICATION`。依 AGENTS.md 第 3 节，在 Q-A01～Q-A06 的产品/接口含义确认前暂停相关实现。确认后由 PM 建立本地固定基线、独立工作区及正式合同，再派发 Code Builder；不重复询问已获得的实施授权。

供用户整体选择或逐项修改的建议（尚未批准）：

1. **Q-A01 权重**：保留原始文件及摘要，以十进制精确乘 100 转为整数；保持权重比例，不重新调难度。
2. **Q-A02 库存**：可分配池只包含 Pending、Active 和 Buffer；扣除其他订单尚缺的数量，已进入订单的食材不再计入。成本计算也必须尊重这份预留，具体稳定分配顺序随公共合同明文固定。
3. **Q-A03 回放**：在点击之外记录实际供给提交及其逻辑边界、暂停/恢复等影响输入接受的边界；回放消费记录，不重新求解物理。Director 的 v 使用 ActiveAvailable 的逻辑数量，遮挡只作为点击命中事实。
4. **Q-A04 确定性协议**：建议由实施方制定并版本化 Seed 序列化、PCG32 初始化/有界抽样、单候选随机消耗、规范化哈希字段及预留分配的技术细则，开工前写入 K0。成本扫描以整盘为单位，包含最后命中盘的全部非目标食材。重试次数、平台信息、表现随机和拒绝输入诊断不进入核心比较哈希。
5. **Q-A05 失败与时钟**：溢出点击是 accepted，计玩家点击次数，不计已处理件数；释放临时 Reserved，物品仍留原盘。0.20 秒输入锁使用由调用方提供的单调游戏时间，暂停时冻结，恢复后走完剩余锁时；回放保存对应时间边界。
6. **Q-A06 连锁**：每结算一个完成订单计一次；单事务允许最多 65 次，尝试第 66 次前进入 Aborted 并输出诊断，避免将空扫描轮次或吸收件数混入计数。

这些建议涉及实际玩法、统计与复现边界；用户的实施请求不被解释为已经逐项选择了原草案明确标为待定的方案。当前没有运行游戏 QA，也没有 Unity/微信运行通过结论。
