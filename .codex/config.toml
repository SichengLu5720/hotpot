[agents]
enabled = true
max_concurrent_threads_per_session = 4

[agents.code_agent]
description = "Implements scoped code work packages and may integrate code-owned shared files."
config_file = "./agents/code-agent.toml"

[agents.visual_agent]
description = "Creates previews, production assets, presentation implementation, and visual integration for scoped work packages."
config_file = "./agents/visual-agent.toml"
