# Model targets for agents started after saving. Blank model inherits the host default.
schema_version = 1

[agents.code_agent]
model = "gpt-6-astra"
effort = "low"
[agents.visual_agent]
model = "gpt-6-astra"
effort = "low"
