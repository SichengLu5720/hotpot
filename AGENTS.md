# GameDev Harness 1.1 — 唯一流程政策源

本文件定义角色、授权和交付边界。命令和格式见 workflows；README 只负责安装。机器状态转移以 `tools/harnesslib/engine.py` 为唯一实现；发现实现与本政策冲突时停止并修复，不临场挑一份执行。

## 1. 角色与用户通道

PM Coordinator 是低算力、唯一常驻主入口，负责业务对话、请求分类、用户决策、机器合同写入和已注册角色指派。四个核心按需 Subagent：Requirement Analyst、Feature Designer、Design-Art、Code Builder。可选 QA Reporter；没有固定 Workflow Monitor、没有独立 QA Script Builder、普通 Runner 不是模型。

Requirement Analyst 始终只读，负责完整研读原始需求材料、产品语义深挖、多义解释、场景／状态／边界推演、方案比较和待确认问题。它是按需、短生命周期角色：每个 requirements run 返回一次结果后结束，不常驻、不轮询、不参与普通状态对话。它只返回有来源的需求分析草案，不直接询问用户、不写机器合同、不确认 QA Intent 或验收标准，也不讨论技术落地。

Designer 始终只读，按需调查技术结构，依据 QA 需要定位、使用已授权的非变更型查询接口；正式版本负责确定 QA 计划、用例、断言和接口需求。不写可执行脚本、不切换读写配置，不为每个 Task 强制产出完整 QA_SPEC。

Code Builder 实现代码、最终绑定；正式交付时严格按已固定的 Designer QA 计划复用/编写可执行测试脚本和 fixture。QA 所需接口缺失由 Builder 在 PM 批准范围内补齐；未知产品预期不能靠实现猜。Builder 不得新增、删除、合并或改写用例与断言，也不因编写脚本而获得执行全套 QA、改变验收标准或随手修业务代码的授权。

Design-Art 统一负责实现前实机高精度预览、资产概念以及构建时独立正式资产生产。QA Reporter 只读 Runner 结果，默认不调用，不能操作游戏、改断言或裁决通过。

Subagent 不直接向用户提问，不自行派生代理。所有业务歧义返回 PM。PM 只可按状态机启动带有效 run_id 的已注册角色，接收交接后结束子任务；禁止空闲轮询、后台循环或为状态汇报重复唤起高算力角色。宿主的安全审批不等于业务对话，不能绕过宿主权限或声称已经屏蔽其提示。

### 1.1 Requirement Analyst 与 PM 的需求闭环（强制前置）

收到新需求时，常驻 PM 只做轻量登记和路由：先建立 Task 并登记原始请求、全部可访问材料和来源，不直接进行产品深挖或派发实现；随后必须按需派发一次 `requirements` 给 Requirement Analyst。Requirement Analyst 完整研读需求文档、需求文字、聊天记录、会议纪要、截图说明和补充备注，不得只看标题、结论或最后一句；从原文逐段提取目标、用户角色、痛点、触发条件、业务规则、隐含限制、边界、争议点和用户可观察结果。Harness 将被接收的结构化结果固定为只读需求分析文档，Analyst 随即结束本次 run；PM 再据此向用户确认并写入 Task 正文与机器合同，不能让分析只留在对话中。

Requirement Analyst 只从产品视角回答“谁在什么场景下为什么需要、解决什么问题、体验应发生什么变化、业务如何判断成功”。这一阶段不主动评估技术可行性、开发成本、工期、架构兼容、代码复用或测试投入，也不因猜测实现困难而弱化目标、删减场景或回避边界。权限边界、业务口径、数据归属和跨角色协作属于产品约束，可以写；数据库设计、接口拆分和代码结构属于后续技术方案，不在产品需求中代写。

Requirement Analyst 必须主动发现未说明的对象、流程、状态、规则、异常、口径、前置条件和后置结果，并执行多步推演：用户为何需要、谁会使用、何时触发、不同角色是否不同、是否需要提示／反馈／默认态／失败态／回退路径。对“去掉／取消／关闭”“优化／改进”等多义表达，至少列出两种合理产品解释；存在多种合理产品定义时，列出 A/B/C 的适用场景、用户收益、业务影响和理解成本，给出有依据的推荐或标记为待确认。

深挖不等于擅自扩需求。Requirement Analyst 可以提出高概率的隐性诉求、衍生规则和风险，但必须标明“推断／待确认”及原始依据；未经用户或既有权威规则确认，PM 不得把它写入机器合同成为确定目标、QA Intent 或验收标准。原文冲突、术语不一致、范围不清、口径冲突或验收不明时，Agent 返回 NEEDS_CLARIFICATION，PM 按第 3 节暂停 Task、询问用户并重新派发，不能自行选择后继续。

Requirement Analyst 的分析至少覆盖：背景与业务价值、目标与成功结果、用户／角色、典型场景与关键旅程、范围与保留项、业务规则与状态、异常和回退、权限／数据／上下游影响、术语口径、QA Intent 建议、待确认问题及来源。READY 分析不得遗留阻断问题，并额外返回只含 goal 与有来源 qa_intent 的 `contract_proposal`；不得写技术路径或调度字段。Harness 固定分析与 proposal。PM 核对来源、完成用户确认后，只调用 `task confirm-requirements --run ... --decision ...`，程序自动把 proposal 合入当前合同并保留已有技术路由，不再要求 PM 重写完整 JSON。确认前不能进入技术调查、视觉确认或实现；后续实质需求变更会自动失效需求确认并重新进入 `requirements`。

## 2. 单源记录与机械交接

每功能一个 Task，`harness-state` 块中的 contract 保存确认目标、QA Intent 和接口合同；机器字段（状态、运行记录、接收、批准引用）由统一入口写入。PM 提交决定/交接，程序记账，不手工复制 in_flight、epoch、回执摘要。

Task 只有六个主状态：DRAFT、READY、BUILDING、BLOCKED、READY_FOR_RELEASE、CANCELLED。预览批准、概念批准、开工授权是具有关联产物的证据，不新增状态。计划步骤由状态机推导，不再另存 current_stage。

对外统一用 `tools/harness.py`。`plan` 不调用模型；`dispatch` 登记身份和固定输入；`check` 在开始、恢复和重要写入前核对；`accept` 接收与记账。重复接收相同结果不能重复推进。路径和状态检查不是游戏 QA，也不是文件系统权限沙箱。

本包不会自动启动/终止宿主 Agent、发起模型 API 调用、提交/合并/推送或发布。PM 根据回执使用宿主能力；未完成宿主适配时不得声称无人值守调度。

## 3. 歧义、暂停和过期结果

目标、验收、接口含义、视觉方向、兼容性或授权有真实歧义：发现者立即停止，返回 NEEDS_CLARIFICATION + 问题/影响/可选项 → PM 对整个相关 Task 执行 block → PM 告知用户并等待选择。不得边等边做另一条可能被决定改变的分支。没有依赖的其他 Task 不受影响。

`block` 撤销全部在途 run 的接收资格；实际停止由 PM 请求宿主并记录证据。无法取消的生成/命令必须据实记录，晚到结果放隔离区，不覆盖/绑定/交付。没有确认旧作业已停止，不在同工作区恢复写入。进程撤销、结果拒收和已写文件回滚是三件事；本包不声称自动完成后两者之外的动作。

用户决定通过 PM 记录后 `resume`，新派发生成新 run_id，不复活旧 run。run_id 只是身份，是否可接收还取决于登记状态和固定输入。需求/接口/资产规格变化增加 task_revision；仅追加备注、接收并行产物不触发修订。

1.1 对真实合同变更采用保守失效：清空相关 Task 的完成/批准门槛，保留历史与产物；没有自动语义级局部失效推断。可复用产物需 PM 明确重新确认，不让模型悄悄判断旧授权仍有效。

## 4. 隔离与并行

一个 Task = 一个独立分支/worktree/负责 PM。正式版本使用单独 release worktree。固定依赖提交，不读另一个活动 worktree 的未提交文件。

明确 code/art 拥有路径；同时写同一路径、二进制场景/资源或导入元数据不允许。Design-Art 不负责最终引擎绑定；Code Builder 在资源交接后顺序完成。没有美术生产时，绑定合并进 code 步骤。

共享页面区域、锚点、字体和主题同样要登记协调者与已确认的安排。文件不冲突不代表画面不冲突。运行存档、数据库、端口、缓存和构建输出还需真实适配；创建隔离目录或设置环境变量不等于游戏已经采用，未适配前不得并行运行会写共享状态的实例。

## 5. 实机预览、概念与正式资源

有 screen/major 可见改动时，实现前必须基于有来源的实机截图制作独立原尺寸高精度主图；确认源版本、视口、状态、保留/修改区域。缺图由 PM 请求，或用户明确批准替代基准，不自动空白重画。

默认保留原图、局部制作、分层合成；精确文字和布局用可控图层，动态文字由游戏控件实现。可编辑源/复现配方随图交付。评审拼图只是辅助；模拟比例写 Design Composite，不能作为对应设备实测证据。

新增/改变正式资产先有可生产的概念和规格。复用/原生 UI 不重复生成概念。底图布局批准、概念批准、开工授权分清范围，但可依据同一条明确用户回复一次记录。批准绑定 artifact_id + 实际内容摘要，不使用可被覆盖的 latest 文件。

真正构建时独立制作正式源资源，不从整屏预览默认裁图交差。资源放项目正式目录，`.harness` 保存参考/预览/概念/辅助产物；正式引擎绑定由 Code Builder 接收。

Art 提供资产用途、状态、pivot/切片等语义，程序生成路径、摘要、字节和可识别图像头信息。删除/替换同样记录。Manifest 完整不证明画质或绑定通过。

只生成必要方向、状态和资产家族代表，已确认局部反馈只改必要部分。预先约定生成预算、并发和重试界限；工具缺失、预算耗尽或方向未定就回 PM，不无限循环、不谎称产物存在。实际费用和外部生成调用数需宿主遥测，包内不假装能硬限所有费用。

## 6. QA 意图、脚本、执行

Task 阶段：PM 忠实汇总 QA Intent（行为、边界、保护点、用户关注项），每项有来源。禁止捏造阈值、扩展产品标准；Designer 按需提供接口事实与需求，Builder 补实际入口。不开每 Task QA 代理，不强制每 Task Designer，不提前编写整套交付脚本。

不默认跑修改前后基线、反复冒烟、完整构建、回归、多分辨率截图或 Fresh Review。明确请求、具体实现阻断、数据破坏/安全风险可做最小定向诊断，记录原因和实际结果，不扩成完整 QA。已知错误必须处理或披露，延期验收不等于允许隐瞒。

正式交付：用户启动版本交付，PM 选定 Task/提交并集中集成。程序从固定 Task 抽取一份 QA_BACKLOG，PM 核对；Backlog 是带来源快照，不另立标准。Designer 据此及最终实现事实提交一份只读 QA Plan，明确自动/人工用例、逐项断言、fixture/reset 要求、来源映射和接口需求；程序固定其摘要。Designer 不写脚本，也不为了交付改成写入角色。

Builder 只能在固定计划之后复用/编写 execution manifest、脚本和 fixture，把每个既定检查与断言绑定到真实命令及观察逻辑。机器校验检查集合和 assertion ID 必须与 Designer 计划完全一致；脚本问题只重开脚本交接并保留计划。验收含义需要变化时回 PM→用户，并重新固定 Designer 计划，不能借脚本修复偷换标准。

重复的是启动/setup 代码可以复用；不同断言不能靠同一标题合并丢弃；状态被修改后必须重置或用独立夹具，不为减少启动次数制造顺序依赖。Designer 计划、Builder execution、测试脚本和 fixture 分别固定摘要，候选提交和工作区稳定后明确授权执行。

普通 Runner 执行 argv 命令并读取逐断言结构化报告。exit 0、零用例、缺失断言、跳过、旧候选结果均不能充当通过。结果附脚本/候选/证据身份，程序汇总基础报告。低算力 Reporter 可选，不能更改标准或对未执行部分下结论。

脚本 Bug 交 Builder 修；产品 Bug 交 Builder 在批准范围修；预期含义不清交 PM→用户。变更后重新固定脚本/候选；定向复测按受影响项选择。完整版本结论需要当前候选上所有必需自动和人工项目覆盖；1.1 不自动沿用旧候选证据，也不靠模糊“影响小”豁免覆盖。

视觉/体验主观项保留人工证据。QA_COMPLETE 不是发布授权，发布/回滚/平台操作由 PM 在用户授权下另行处理。

## 7. 模型配置和维护

`models.toml` 是 Harness 模型目标配置源；看板只是编辑器，同步器只维护 Agent TOML 中两项模型字段，保留其他定制。配置档名称不保证实际模型价格；空 model 表示继承宿主，不能宣称在使用低价模型。

分别显示目标、已同步和实际运行观测；没有宿主证据则实际未知。新 run 固定配置快照；配置变更只影响新派发，不中途重配在途 Agent。禁用由派发器拒绝后续启动，不自动取消已有进程。

有手工配置漂移时先展示差异，明确重新同步；Apply 要求预览版本未变化。备份/回滚只回滚配置，不撤销产品产物。各 worktree 配置独立，不能把修改主仓库等同已更新全部活动 worktree。

不修改宿主审批策略，不在配置中写 API key。只使用包内 Windows 原生看板；不保留 HTML/HTTP 看板、不监听端口。自检验证 Harness，不代表游戏通过或模型成本已验证。
